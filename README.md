# software_factory_beta

Small zero-dependency Node.js test app used to try out the Eve software factory
(agent pipeline triggered from GitHub).

## Usage

```bash
npm start   # serves "Hello world" on http://localhost:3000
npm test    # node:test suite
```

## Endpoints

- `GET /` → `200 text/plain` `Hello world`
